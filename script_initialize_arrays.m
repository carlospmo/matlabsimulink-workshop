%%% Declaring arrays using built-in functions

% This program shows how to initialize arrays using the different built-in
% functions available in MATLAB

a = zeros(2);
b = zeros(3,2);
c = [1, 2, 3; 4, 5, 6];
d = zeros(size(c));

e = ones(3,5);

f = eye(2);
g = eye(2,3);